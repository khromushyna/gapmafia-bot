services:
  - type: worker
    name: gapmafia-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python mafia_bot.py
    envVars:
      - key: TOKEN
        sync: false
